"""Placeholder lambda function for the VPC Manager API."""

def lambda_handler(event, context):
    """Handle incoming requests to the VPC Manager API."""
    return {
        "statusCode": 200,
        "body": "Hello from the VPC Manager API!"
    }